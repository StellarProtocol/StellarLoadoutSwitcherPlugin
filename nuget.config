<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
    <!-- DEV-ONLY scaffolding: resolves the unpublished Stellar.Abstractions 2.3.0 from the devkit local
         feed while the framework release is in flight. REVERT before release (CI resolves 2.3.0 from
         nuget.org once published) — see plan Task 9 / ledger. -->
    <add key="localfeed" value="../../.localfeed" />
  </packageSources>
</configuration>
